local const = {
    SCREEN_WIDTH = love.graphics.getPixelWidth(),
    SCREEN_HEIGHT = love.graphics.getPixelHeight(),
    GAME_WIDTH = love.graphics.getPixelWidth() / 4,
    GAME_HEIGHT = love.graphics.getPixelHeight() / 4,
    SCALE_FACTOR = 4
}

return const